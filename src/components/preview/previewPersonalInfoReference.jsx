const PreviewPersonalInfoReference = props => {
  return (
    <>
      <h2 className="preview__body-headText">REFERENCES</h2>
      <p>{props.data.references}</p>
    </>
  );
};

export default PreviewPersonalInfoReference;
